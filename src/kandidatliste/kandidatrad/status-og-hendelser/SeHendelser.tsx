import React, { FunctionComponent } from 'react';
import { Utfall } from '../utfall-med-endre-ikon/UtfallMedEndreIkon';

type Props = {
    utfall: Utfall;
};

const SeHendelser: FunctionComponent<Props> = (props) => {
    return <div>Se hendelser</div>;
};

export default SeHendelser;
